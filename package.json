{
  "name": "node-ecs-deploy",
  "version": "1.0.0",
  "description": "Express app with random greeting and health check endpoints",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon index.js",
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "@vercel/ncc": "^0.38.1"
  }
}
